services:
  spook-shack-discord:
    image: ghcr.io/pdpablo/spook-shack-discord:latest
    restart: unless-stopped
    env_file:
      - .env
    environment:
      KAMANOSUKE_HOME: /opt/data
    volumes:
      - ./data:/opt/data
