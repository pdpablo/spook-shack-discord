services:
  spook-shack-discord:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      KAMANOSUKE_HOME: /data
    volumes:
      - spook-shack-data:/data

volumes:
  spook-shack-data:
